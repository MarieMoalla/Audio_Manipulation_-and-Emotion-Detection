using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using PythonScripts;

public class Validate : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        PythonManager.RunPart1(); 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
